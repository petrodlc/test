---
title: Switching between versions
layout: page
parent: Installation
---

# Switching between versions
You can changed the version used by changing the git branch.

#### Switch to `dev` (unstable) version
```shell
git checkout dev
```

#### Back to stable
```shell
git checkout main
```
